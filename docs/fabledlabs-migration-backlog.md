# FabledLabs Migration Backlog

## Alkemist Backlog

1. Done: added labs/alkemist module boundary.
2. Done: added sandbox profile presets and workspace templates.
3. Extract Build mode state/actions into smaller Alkemist components.
4. Add design-preview bridge to Tesseract for rapid prototype handoff.
5. Align AI profile orchestration with Alkemist repo architecture patterns.

## Scribe Backlog

1. Done: added labs/scribe module boundary and course lane blueprint.
2. Extract School mode and LearnPanel into smaller Scribe components.
3. Formalize lesson schema versioning.
4. Add authoring pipeline for lesson packs.
5. Add progress and completion telemetry hooks (local-first).
6. Add import/export workflow for coursework bundles.

## Tesseract Backlog

1. Done: added labs/tesseract module boundary and primitive blueprint.
2. Extract Design page into smaller Tesseract components.
3. Define floor-plan primitives: wall, room, door, window, prop.
4. Add grid snap, orthographic plan view, and dimension labels.
5. Add lightweight character placeholders and avatar placement.
6. Add blueprint export to SVG/PNG and shareable JSON scene format.

## Logix Backlog

1. Done: added labs/logix module boundary.
2. Done: moved block templates, starter chain, and validation into Logix flow model.
3. Refine node taxonomy and execution contract.
4. Add cycle detection diagnostics.
5. Add simulation runner for route testing.
6. Build import bridge for legacy flow structures inspired by FabledFlow patterns.

## Cross-Lab Backlog

1. Build shared design system tokens and component kit.
2. Split shared utilities into packages/shared-core.
3. Add per-lab release channels and packaging config.
4. Add integration contracts for handoffs between labs.
5. Add end-to-end smoke tests for all four labs.
